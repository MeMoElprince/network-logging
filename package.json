{
  "name": "network-logging",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -n relay,web -c blue,green \"npm run relay\" \"npm run web\"",
    "relay": "npm --prefix relay run dev",
    "web": "npm --prefix web run dev",
    "build:ext": "npm --prefix extension run build",
    "install:all": "npm install && npm --prefix relay install && npm --prefix web install && npm --prefix extension install"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
